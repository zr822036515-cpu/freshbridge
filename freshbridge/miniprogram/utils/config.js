// API configuration — change BASE_URL for different environments
const config = {
  // 生产环境
  baseURL: 'https://api.freshbridge.cn/api',
  // 测试环境: 'https://test-api.freshbridge.cn/api'
  // 本地开发: 'http://localhost:8080/api'
  timeout: 15000
}

export default config
